#ifndef _MathUtils
#define _MathUtils

#include <cmath>

class MathUtils
{
public:
    static double DegreeToRadian(const double & alpha);

    static double RadianToDegree(const double & alpha);

    static double Pi();
};

#endif // MATHUTILS_H
